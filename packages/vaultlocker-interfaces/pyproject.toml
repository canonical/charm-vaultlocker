[project]
name = "vaultlocker-interfaces"
version = "0.1.0"
description = "Helpers for Vaultlocker relation interfaces."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
 "ops~=3.7",
]

[build-system]
requires = ["uv_build>=0.9.9,<0.10.0"]
build-backend = "uv_build"
